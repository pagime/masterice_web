doctype html
html(lang="en")
    head
        title=Title
        script(type='text/javascript').
            if (foo) bar(1 + 5)
    body
        h1 Contenu du fichier
        #container.col
        br